# Fabricator Pro — Logikal / Klaes audit and upgrade plan

**Decision:** retain and harden the current platform. The inspected implementation does not yet justify a claim of production equivalence to Logikal or Klaes. Prioritize manufacturing correctness, revision integrity and release enforcement before expanding the feature catalogue.

**Scope and evidence — 20 September 2026**

| Evidence | Result / limit |
|---|---|
| [Supplied Vercel deployment](https://vercel.com/mohamed-hassans-projects-0ca04921/almona-portfolio-forge/GQZ9PnwFhxVwuZ4ukovkFCe1SMj7) | Browser inspection: Ready, Stale, Production; source `db4ac0f3ee2ddffabf01dc2592859cd8c2844c4a`, branch `codex/public-site-launch`. Deployment Checks says **No checks configured**. This does not prove that GitHub has no checks. |
| [Deployment URL](https://almona-portfolio-forge-3yezl61t7.vercel.app/fabricator/studio/command) | Loads, then redirects to login. Authenticated workflows, mobile usability and production API health were not verified. |
| [Repository](https://github.com/almona02/almona-portfolio-forge) | Local HEAD `a7b5db793a205d1fa0fd97fc7b3797aefc71a37c`, two commits after deployment. Relevant Fabricator files, export API, workflow store and inspected CI definitions have no changes between these revisions. Later changes include database privileges, quote handling and database types; they are not assumed deployed. |
| Architecture | React 18, Vite, React Router, Zustand, React Query, Supabase/PostgreSQL and Python FastAPI. Next.js/RSC/Prisma are not the implementation inspected. |
| Validation executed | 80 tests passed across 11 files; an additional operator/isolation-filtered run passed 1 test, skipping 17 unselected tests. Four isolated frontend probes completed. |
| Type checking | Root `tsc --noEmit --listFilesOnly`: exit 0, **zero files listed**. Explicit application check: exit 2, four syntax diagnostics at `ManufacturingSettings.ts:59–60`. Further type errors may be hidden by this parsing failure. |
| Limits | No machine import or physical cut, competitor application session, full regression/build, deployed database isolation test, or authenticated end-to-end test. Export API execution probe could not import FastAPI in the available Python environments; export findings below are source-confirmed. |

Competitor capabilities below are documented by their vendors, not independently benchmarked here. Modules, licensed databases and machine commissioning affect actual availability. This report deliberately avoids numerical “percentage parity” claims.

**Competitive comparison**

Logikal is the more useful reference for supplier-driven construction rules and design-to-machining continuity. Its documentation describes compatible-item selection, CNC machining definitions and BIM interfaces. [Logikal product](https://www.logikal-software.com/en/product/logikal), [technical FAQ](https://www.logikal-software.com/en/faq), [CNC documentation](https://help.orgadata.com/en/documentation/cnc/programming-basics/create-machining/).

Klaes is the more useful reference for connected order processing, procurement, capacity planning and factory execution. Its product portfolio documents invoicing, material ordering, workstation scanning and machine control. [Klaes workflow overview](https://portal.klaes.de/en-tour).

| Capability | Logikal reference | Klaes reference | Fabricator evidence and gap | Priority |
|---|---|---|---|---|
| System and supplier data | Supplier catalogue with compatibility checks | Supplier configurators and material workflows | Regional system packs, profiles and constraints exist. Supplier-approved revisions, compatibility coverage and update qualification were not demonstrated. | P1 |
| Design and detailing | Rule-aware element design and machining | Window/door design, sections; separate 3D offering | SmartDraw, drafting, 3D and constraint modules exist. Preserve them; qualify supported configurations rather than equating visual output with manufacturing approval. | P1 |
| BOM and cutting | Construction feeds production data | Material and labour calculation feeds production | BOM generator, deterministic solver and kerf tests exist. Stale state, incomplete cache identity and disconnected export preflight weaken the complete chain. | P0 |
| Machine output | Profile-referenced operations, faces and machining parameters | Machine control and on-site setup | G-code generation exists. Inspected ALM 6510 path produces JSON; machine sending is simulated; partial export can omit rejected cuts. | P0 |
| Quotes and commercial | Calculation and ERP interfaces | Quotes/orders, invoices and credit notes | Quote UI and wider invoice modules exist. Pose quote save is local workflow state; pricing provenance and approved revision linkage need completion. | P1 |
| Inventory and procurement | Ordering / ERP interfaces | Stock, receipt and electronic ordering workflows | Stock-movement RPCs and remnant lifecycle exist. Prove atomic linkage between production completion, consumption, reservation and purchasing. | P1 |
| Capacity and shop floor | MES option | CAPA and e-prod | Execution tables/services and scheduling algorithms exist. No application callers found for the three inspected scheduling implementations; operator identity and transactional completion need work. | P1 |
| Engineering evidence | Technical rules and specialist interfaces | Technical / declaration modules | Structural and thermal modules exist, but EngineeringBay uses assumed loads and thermal areas. Treat results as estimates until project-specific inputs and validated evidence are attached. | P1 |
| BIM and external systems | IFC, Revit and ERP interfaces | ERP/accounting interfaces and specialist modules | IFC exporter is a scaffold; SAP/Odoo page explicitly says planned. Real interoperability is a later workstream. | P2 |
| Regional operations | Validate required regional modules in procurement | Validate required regional modules in procurement | Arabic/English resources, Egyptian packs and EGP defaults are useful foundations. Arabic workshop workflows plus Almona machine service/passport integration are a proposed differentiation, not a proven comparative advantage. | P1/P2 |

The specific Logikal BIM reference documents IFC and Revit interfaces. [Orgadata BIM](https://help.orgadata.com/en/documentation/interfaces/bim/). Klaes CAPA documents forward/backward/fixed-date scheduling and shared planning views. [Klaes capacity planning](https://www.klaes.de/en-capacity-planning).

**Actionable audit findings**

P0 = release blocker for the affected manufacturing capability. P1 = required for a reliable commercial pilot. P2 = expansion after the production path is qualified.

| ID | Severity | Evidence and consequence | Required correction |
|---|---|---|---|
| F01 | P0 | [ProductionCommand:361](/C:/projects/almona-portfolio-forge/src/components/fabricator/ProductionCommand.tsx:361) simulates sending with a delay; the action dispatcher subsequently reports “Job sent”. A failed MDB download is caught internally, while its caller can still set a success result. These are source-confirmed false-success paths. | Disable live-send claims until a real transport supplies an acknowledgement. Propagate export errors. Separate generated, downloaded, transmitted, accepted and completed states. |
| F02 | P0 | [ALM 6510 API:265](/C:/projects/almona-portfolio-forge/python_backend/apis/v2/alm6510_export.py:265) catches per-cut conversion errors and continues when other records remain. It returns JSON, and [frontend exporter:98](/C:/projects/almona-portfolio-forge/src/lib/exports/ALM6510MDBExport.ts:98) downloads `.mdb.json`. This is not a binary MDB encoder, despite machine-ready UI wording. | Reject the entire production export on any invalid/missing cut. Label JSON as an intermediate/debug format. Enable machine-ready MDB only after encoder, schema and actual controller-import acceptance. |
| F03 | P0 | [Workflow store:146](/C:/projects/almona-portfolio-forge/src/store/workflowStore.ts:146) changes design/current project without invalidating BOM, optimization, quote or documents. An isolated probe switched pose A to B and retained A's cutting plan and quote; a subsequent design edit retained the old plan. The store uses one persistent key. | Scope state to authenticated owner, project, pose and revision. Atomically invalidate dependent results after upstream edits. On reload/deep links, hydrate the requested pose and reject mismatched artifact revisions before export. |
| F04 | P0 | [Machine export preflight](/C:/projects/almona-portfolio-forge/src/lib/fabricator/production/machineExportPreflight.ts:38) has test callers but no production caller found. Active G-code validation examines supplied placed plans; the inspected MDB path checks nonempty data. This does not establish conservation of the original required parts. | Apply one server-enforced release gate to every export: required piece identity/quantity, placed quantity, both angles, stock balance, unplaced cuts, approved settings and revision hash. Keep existing deterministic algorithms. |
| F05 | P1 | [Package scripts](/C:/projects/almona-portfolio-forge/package.json), [root config](/C:/projects/almona-portfolio-forge/tsconfig.json), and [invalid mapped interface](/C:/projects/almona-portfolio-forge/src/lib/fabricator/ManufacturingSettings.ts:58): configured root checking succeeds without checking source; explicit app checking fails. Vercel has no deployment checks configured. | Check app and tooling projects explicitly; fix surfaced errors without relaxing strictness. Require the resulting checks on the exact commit promoted to production. Cover the actual release branch. |
| F06 | P1 | [BOM generator:146](/C:/projects/almona-portfolio-forge/src/lib/fabricator/PresetAwareBOMGenerator.ts:146) keys a five-second cache by IDs, grid, component count and dimensions, omitting full component data, hardware, glazing and price revisions. It also sets `accuracy` from a fixed `0.998` target at line 207. | Hash all BOM-affecting inputs and rule/catalogue/price versions. Test same-count component changes and glazing/hardware edits. Separate measured validation evidence from configured accuracy targets. |
| F07 | P1 | [Workflow validator:72](/C:/projects/almona-portfolio-forge/src/lib/fabricator/validation/WorkflowValidator.ts:72) returns valid for unknown steps. `commercial` is missing from its step type/order even though later code handles it. An isolated empty-state commercial call returned valid. The other validator API has different prerequisites; the layout validation banner does not itself block its outlet. | Define one exhaustive step model and shared blocking policy. Reject unknown steps; test direct navigation, missing BOM/optimization, expired approvals and stale revisions. Existing page-level blocking must remain. |
| F08 | P1 | [CostCalculator:65](/C:/projects/almona-portfolio-forge/src/lib/fabricator/bom/CostCalculator.ts:65) declares pricing context but never populates it in this path. [QuoteBuilder:52](/C:/projects/almona-portfolio-forge/src/components/fabricator/workflow/QuoteBuilder.tsx:52) saves a pose quote to Zustand. Wider commercial/invoice modules do exist; their presence does not establish an approved pose-to-invoice transaction. | Persist quote revisions with source BOM/optimization hashes, currency and dated price inputs. Use explicit money-rounding rules. Connect acceptance, order and invoice references without duplicating the existing commercial subsystem. |
| F09 | P1 | [ExecutionService:75](/C:/projects/almona-portfolio-forge/src/services/executionService.ts:75) uses literal `current_user` as operator and writes logs/consumption before the separate stage update. Partial failures can leave inconsistent records; the tested service uses mocked Supabase. | Resolve operator identity server-side. Use an idempotent database transaction for transition, stock movement and audit event. Verify retries, concurrent scans, rollback and cross-owner access against real test tables. |
| F10 | P1 | [EngineeringBay:175](/C:/projects/almona-portfolio-forge/src/components/fabricator/EngineeringBay.tsx:175) uses assumed wind inputs and fixed glass/frame areas. [ThermalEngine](/C:/projects/almona-portfolio-forge/src/lib/physics/ThermalEngine.ts) accepted impossible geometry in an isolated probe, producing a negative perimeter. | Require finite positive dimensions and valid material/system boundaries. Derive areas and loads from the approved project or show an explicit estimate. Attach validated supplier properties and engineering review before presenting compliance conclusions. |
| F11 | P2 | [BIMExporter:32](/C:/projects/almona-portfolio-forge/src/lib/exports/BIMExporter.ts:32) refers to undeclared `#20/#30` entities and returns a fixed GUID. No application consumer was found. [Integrations page](/C:/projects/almona-portfolio-forge/src/pages/fabricator/studio/StudioIntegrationsPage.tsx:50) marks SAP/Odoo planned. | Keep unavailable capabilities explicit. Implement valid IFC geometry, unique identifiers and schema/viewer tests; build one contracted ERP integration with retry, deduplication and reconciliation. |
| F12 | P1 | [LeanScheduler](/C:/projects/almona-portfolio-forge/src/modules/manufacturing/LeanScheduler.ts:35), [MultiMachineScheduler](/C:/projects/almona-portfolio-forge/src/optimization/yilmaz-specific/MultiMachineScheduler.ts:55), and the genetic scheduling module have no application callers found in the source search. A scheduler component is largely status/start controls. | Consolidate behind a scheduling service with persistent resources, calendars, material readiness, dependencies and machine downtime. Keep advisory schedule optimization separate from authoritative cutting calculations. |

**What should be preserved**

The canonical Studio route hierarchy, regional packs, SmartDraw/drafting/3D components, supplier-profile hooks, stock-movement RPCs and remnant lifecycle are reusable foundations. V2 schemas and queries include owner scoping and row-level security; live enforcement still needs integration verification. Deterministic manufacturing tests, explicit unsupported NCW handling and the existing evidence/provenance work are valuable controls.

The DoWin tests deliberately preserve unresolved external evidence. [Golden replay test:151](/C:/projects/almona-portfolio-forge/src/tests/fabricator/fp024c13GoldenReplay.test.ts:151) asserts no canonical production integration and an unproven FP-027 root cause. A passing test preserves that boundary; it does not establish machine parity. The older FP-027 forensic report concerns observations from a licensed comparator workflow and must not be relabelled as a newly reproduced Fabricator solver defect.

Older [Logikal gap documentation](/C:/projects/almona-portfolio-forge/docs/LOGIKAL_GAP_ANALYSIS_AND_WIRING_PLAN.md) and [competitive summaries](/C:/projects/almona-portfolio-forge/docs/COMPETITIVE_REALITY_VS_DOCUMENTATION_EXECUTIVE_SUMMARY.md) contain completion/accuracy/parity percentages. They are not acceptance evidence for this release.

**Upgrade sequence**

Planning assumption: three software engineers, one QA engineer, and part-time access to an Almona fabrication engineer, supplier data owner and machine technician. Windows below are indicative; the release gates determine promotion. This is a bounded pilot plan, not a promise of full Logikal/Klaes parity in 90 days.

| Window | Modular work packages | Responsible roles | Exit evidence |
|---|---|---|---|
| Weeks 1–2: contain risk | A: accurate export/send states and all-or-nothing export. B: real TypeScript checks and mandatory release checks. C: owner/pose/revision-scoped workflow with downstream invalidation. D: shared workflow/export validation. | Frontend, backend, QA | Reproduce F01–F07 as regression cases; fixes pass. A failing check blocks promotion of that exact commit. No stale or incomplete artifact can be released. |
| Weeks 3–4: qualify data | E: versioned system-pack and price records, source documents, approval history and compatibility matrix. F: canonical BOM/cache identity and durable quote revisions. | Fabrication engineer, data owner, frontend/backend | Select one commercially important aluminium system and one UPVC system. Approve supported frame/sash/bead/hardware/glazing combinations and manufacturing allowances; unsupported combinations block release. |
| Weeks 5–6: qualify outputs | G: immutable production release record and shared piece ledger. H: one target saw/controller and one machining-center export qualification. | Backend, machine technician, QA | Required parts, optimization, cut sheets, labels and machine records agree by identity, quantity and length. Controller import and measured samples pass the approved tolerance for each operation. No format becomes “ready” from file generation alone. |
| Weeks 7–10: connect the factory | I: transactional operator execution and stock consumption. J: finite-capacity planning connected to material readiness and machine availability. K: quote/order/invoice and purchase references. | Backend, frontend, QA, workshop lead | Duplicate scans/retries consume stock once; failed transactions leave no partial completion. Two users cannot over-reserve the same material. Scheduling honors shifts, dependencies and maintenance. |
| Weeks 11–12: prove the pilot | L: Arabic/English workshop validation, reload/offline recovery, concurrent users, delivery/quality evidence and supported machine passport links. | QA, workshop lead, fabrication engineer | Signed pilot results for the selected systems/machines; support runbook, backup/restore drill, rollback rehearsal and measurable operating results. |
| Following quarter | M: expand qualified supplier systems and controller models. N: one ERP/accounting integration. O: valid IFC/BIM and advanced facade detailing where sales demand supports them. | Product owner plus relevant specialists | Each addition has supplier/customer acceptance and a maintained compatibility matrix; prioritize by actual order demand. |

Use the current React/Vite/Supabase/FastAPI architecture for this plan. A Next.js or ORM migration adds scope without resolving the observed manufacturing defects. Apply new contracts at existing boundaries and preserve public APIs and service models.

The production release record should bind owner, project, pose revision, catalogue/rule versions, manufacturing settings provenance, BOM hash, required/placed piece ledger, price snapshot, controller/postprocessor version, validation evidence and approver. Existing constitutional hashes should be reused where they satisfy this contract. Every document/export must consume the same approved revision; design or settings changes invalidate it.

**Acceptance benchmark**

Use licensed, operator-produced Logikal/Klaes outputs where available, plus supplier drawings and independently measured workshop results. A competitor output is a reference to reconcile, not unquestionable physical truth. Keep unresolved cases blocked; do not alter production formulas to imitate unexplained comparator behavior.

| Benchmark | Proposed acceptance criterion |
|---|---|
| Reference jobs | Initially 30 approved jobs: 10 aluminium, 10 UPVC, 5 multi-position/batch jobs, 5 invalid/boundary cases. Cover different opening types, reinforcement, hardware, both end angles, weld allowances, stock sizes and remnant cases. Increase coverage for each added system/model. |
| Conservation | Exact required/placed/exported piece identity and quantity; zero unexplained extra or missing pieces. Stock balance includes cuts, kerf, trim and remainder under the approved convention. |
| Dimensions | Every piece passes its documented operation-specific tolerance. The existing ±0.1 mm DoWin fixture criterion is scoped evidence, not a universal machine tolerance. Never average away an individual failure. |
| Revision integrity | Editing size, profile, glass, hardware, price or settings invalidates affected artifacts. Refresh/deep link and switching users/poses cannot reuse another revision's outputs. |
| Controller acceptance | Schema-valid file plus successful target-controller import, operation review and measured sample. Offline/disconnected send cannot report acceptance. |
| Pricing | Screen, persisted quote and PDF agree at the agreed monetary precision; taxes/discounts/currency use configured, versioned business rules. This audit makes no tax-compliance determination. |
| Shop floor | Repeated and concurrent scans are idempotent; partial failure rolls back; stock reservations reconcile to completion, waste and remnants. |
| Usability | Observe the same representative jobs with workshop operators in Arabic and English. Record completion time, corrections, failed saves, recovery and training needs before claiming a speed advantage. |
| Release | Correct application/tooling type checks, constitutional/operator tests, physical conservation/export integration tests, production build and authenticated smoke test all pass on the promoted SHA. |

**Validation record**

The main Vitest run selected kerf accounting, Gate 2 manufacturing truth, DoWin physical-length evidence, golden replay, optimizer authority, Studio protection, execution service and drafting constitutional tests: **11 files / 80 tests passed**. The operator/isolation-filtered compensation reconciliation run: **1 passed / 17 unselected tests skipped**. Neither run was a full suite or physical machine qualification.

Isolated probes loaded local frontend modules in memory, without remote writes: switching poses retained the previous plan/quote; editing design retained the previous plan; an empty-state `commercial` validation returned valid; impossible thermal geometry returned `Lg = -0.4`. These prove module behavior, not an authenticated browser reproduction. A first probe launcher failed due to its runtime environment; the probes completed using the installed TypeScript transpiler.

Direct application compilation returned TS1005/TS1128 diagnostics at `ManufacturingSettings.ts:59–60`. The API mixed-valid/invalid-cut probe remained unexecuted because FastAPI was unavailable in the attempted Python environments. The partial-export and JSON findings are established from the inspected handler and client, not from a claimed live API result.

Application code, deployment settings and production data were not changed by this audit. This report is the upgrade backlog and acceptance plan.
