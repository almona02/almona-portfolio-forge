-- 001_initial_schema.sql
-- Consolidated initial schema extracted from database-schema.sql
-- Copy only the DDL (types, tables, rls policies) you want as the baseline.
-- This is a placeholder; populate after verifying Supabase DB host resolves.

BEGIN;
-- TODO: Paste curated content from database-schema.sql here once connection works.
COMMIT;
