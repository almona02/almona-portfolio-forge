# CI-friendly requirements (CPU-only, no heavy GPU/ML wheels)
# Base: production deps to stay aligned with runtime, then CI/test tooling.
# NOTE: Using requirements-prod.txt as base (newer versions) instead of requirements-production.txt

-r requirements-prod.txt

# CPU-only ML to avoid huge CUDA wheels in CI
# CRITICAL SECURITY FIX: Upgraded to 2.8.0 to fix CVE-2025-3730 (DoS vulnerability)
# See: https://github.com/advisories/GHSA-887c-mr87-cxwp
# Previous fix: Upgraded from 2.3.1 to 2.6.0 to fix CVE-2025-32434 (RCE vulnerability)
# Note: +cpu local version label requires == operator, not >=
# torchvision 0.23.0+cpu is compatible with torch 2.8.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu
torchvision==0.23.0+cpu

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-timeout==2.2.0
pytest-mock==3.12.0
locust==2.24.0  # pinned to avoid requests>=2.32.2 conflict with resend 0.6.0
respx==0.21.1
psutil==5.9.6

# Linting/formatting
black==24.10.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Security tools
bandit==1.7.10
pip-audit==2.10.0

# Background tasks (needed for imports in tests)
celery==5.3.4

# Redis (needed for cache imports in tests)
redis[hiredis]==5.0.1
hiredis==2.3.2

# Flask limiter (needed for part_detection imports in tests)
flask-limiter==3.7.0

# JWT (needed for part_detection imports in tests)
PyJWT==2.9.0

# CAD / image processing (lightweight; inherits ezdxf pin from production)
albumentations==1.4.18

