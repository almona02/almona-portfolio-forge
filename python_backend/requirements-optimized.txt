# Optimized Production Requirements - TARGET: ~1.5GB
# Removed: ortools (has fallback), pandas (only required by ortools), sympy (check if needed)

# Core Framework
fastapi==0.123.8
uvicorn[standard]==0.24.0
pydantic==2.9.0
python-multipart>=0.0.20

# Database
supabase==2.8.0
psycopg2-binary==2.9.9
sqlalchemy[asyncio]==2.0.23

# ML Inference (CRITICAL - MINIMAL SET)
tensorflow-cpu==2.17.1  # ~350-400MB (core ML)
onnxruntime==1.20.0     # ~40MB (requires sympy, so sympy stays)

# Computer Vision (MINIMAL)
opencv-python-headless==4.10.0.84  # ~60MB
pytesseract==0.3.10     # Thin wrapper only

# Optimization
# ortools==9.8.3296  # REMOVED: Has fallback solver, saves 139MB (ortools 60MB + pandas 79MB)
# sympy: KEPT (required by onnxruntime)

# Utilities (MINIMAL)
numpy==1.26.4           # ~40MB
requests==2.32.5
pillow==10.4.0          # ~20MB

# Security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
PyJWT==2.9.0

# Monitoring (MINIMAL)
structlog==23.2.0
prometheus-client==0.19.0

# Background Tasks
celery==5.3.4
redis==5.0.1

